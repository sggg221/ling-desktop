# 验证记录

## 设置、截断提示与启动脚本修复

- 新增客户端检查：`finish_reason` 为 `length`、`stop`、`null`、缺失和未知值；截断的文本数组保留内容；保存前的离线配置校验。
- 新增 macOS XCTest：草稿不影响已保存配置、规范化保存、非法配置与钥匙串写入失败不改变现有设置、清除 Key、连接测试使用草稿、编辑/关闭/保存后忽略旧结果、旧测试不能覆盖新测试。
- 新增 Python 启动脚本检查：首次启动、已有构建和较新的源码都会先构建再打开；构建失败不会打开旧版本；项目路径包含空格。
- 本轮编辑环境为 Linux，没有 Swift/macOS SDK。已通过 4 项 Python 启动脚本回归检查、`bash -n build.sh check.sh run.command`、Info.plist 解析和 `git diff --check`，并核对 15 个客户端检查方法均已登记到 runner。Swift 检查、Release 构建、签名及 UI 行为尚待 macOS CI/桌面验收，不沿用下方历史构建结果。
- macOS CI 已配置客户端检查、`swift test`、启动脚本检查、Release 构建与签名校验；配置存在不代表检查已经通过。

### 本次修复的桌面验收

1. 保存有效配置 A，打开设置改成 B，不保存直接关闭；提问仍使用 A，重新打开设置显示 A。点击「取消」或开始截图也应放弃草稿。
2. 在草稿中输入非法 HTTP 远端地址或空模型，保存应报错并保留窗口；提问和重启后仍使用原来的配置。清空 Key 后保存应删除凭证。
3. 用本机延迟响应的测试接口启动连接测试，修改 URL、模型或 Key；进度和旧结果应清除，晚到的响应不得显示成功。关闭后重开、保存期间取消、连续发起两次测试也应如此。
4. 本机测试接口返回 `finish_reason: "length"` 和部分文字，浮窗应同时显示文字和截断提示，复制按钮只复制文字；下一次正常回答、移除截图或重新框选后不应残留提示。
5. 退出应用，更新源码后双击 `run.command`，确认先完成增量构建再打开；人为制造编译错误时不得启动旧构建。已运行的应用需先退出，启动脚本不会强制结束进程。

## 历史验证 · 2026-09-09

以下为上传源码时已有的记录，不代表当前分支重新通过了验证。

## MaaS 接口更新与仓库准备

- 根据使用者提供的地址，将源码默认 Base URL 改为 `https://maas-api.antdigital.com/v1`；最终请求为 `https://maas-api.antdigital.com/v1/chat/completions`。
- 已有设置支持保存完整端点，并且不会再次拼接 `/chat/completions`；自动检查已覆盖新 Base URL 和完整端点两种形式，12 组检查全部通过，Release 重新编译通过。
- 使用者反馈“可以啦”；本轮没有独立重测真实截图与模型回答。下方保留首次验证时的环境记录，不代表当前使用者的桌面仍有同样问题。
- 源码上传范围只包括 Swift 源码、测试、构建脚本、应用元数据及说明；不包含密钥、偏好设置、截图或编译缓存。

## 首次构建：已验证

- 环境：Apple Silicon，macOS 26.5.1，Swift 6.3.3 / Command Line Tools。
- 原生项目 Release 编译通过，生成 `dist/LingDesktop.app`。
- App 包内可执行文件存在且具备执行权限；Info.plist 语法校验和 `codesign --verify` 通过。
- 12 组客户端自动检查通过：图文格式、纯文本及自定义模型、纯图片、空输入、Base URL 规范化、无效配置、401 与 Key 脱敏、404、异常/空响应、文本数组响应、超时、连接测试端点。均为本地模拟 HTTP，不含真实凭证。

## 首次构建：未通过 / 待验收

- 本轮 Codex 终端 `open` 返回 LaunchServices 错误 `-10827`。直接启动二进制在 `NSApplication.shared` 的系统 `_RegisterApplication` 阶段收到 SIGABRT，未进入 AppDelegate。文件与签名存在且有效；尚不能据此保证在普通桌面会话中正常启动。
- Computer Use 返回「未批准使用百灵视觉助手」，没有继续绕过权限操作桌面。
- 当前终端网络无法解析 `api.ant-ling.com`，没有发送真实 Key、截图或伪称获得百灵回答。
- 因此：设置与浮窗视觉布局、钥匙串实际读写、全局快捷键、系统截图权限、框选以及真实模型回答组成的完整闭环，仍需在获准的桌面会话中测试。

## 桌面验收步骤

1. 从 Finder 打开 app：首次设置正常显示，复制/粘贴可用；填写 Key，保留默认地址和模型，点「测试连接」，看到真实成功回答。保存、退出、重开，确认设置已保留。
2. 首次按 ⌘⇧A：无录屏权限时看到说明；手动授权并重开后能够框选。Esc 应取消，菜单栏应继续响应。
3. 框选一个测试文本区域：浮窗缩略图只含所选区域，不含助手本身。输入“提取这里的文字”，发送后核对真实回答。
4. 留空问题发送截图，再移除截图只发文字，分别确认可用。
5. 发送过程中取消、关闭浮窗、重新框选，确认旧请求不会覆盖新截图的回答。
6. 临时填错 Key 或模型：应显示真实 401/404 等服务端状态；恢复正确配置。断网时应显示网络错误，而非假成功。
7. 若有多显示器或不同缩放比例，分别跨屏框选，核对缩略图与选区一致。

只在步骤 1–4 实际完成后，才把 MVP 的核心体验标为验收通过。
